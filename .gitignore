__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
.env
.DS_Store

# Runtime secrets and private data. These normally live in ~/.linkedin-mcp, but
# LINKEDIN_MCP_DIR can point anywhere — including into this checkout — so never
# let a stray `git add -A` stage a live OAuth token or the job-hunt database.
.linkedin-mcp/
tokens.json
state.json
*.db
